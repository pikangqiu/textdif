# AGENT.md — 项目上下文与文档索引

> 给所有 AI / 协作者的第一入口。读完本文即可建立项目全貌、找到所有关键文档、并知道在共享服务器上操作的硬约束。
> 主代码仓：`/data/ywk/VOSR`（分支 `text-distill-ablation`）。最后更新：2026-06-21。

---

## 1. 项目是什么

**VOSR 文本图像超分 · 一步蒸馏（text-SR one-step shortcut distillation）。** 目标：把多步 flow-matching 的文本 SR 教师模型蒸馏成 **NFE=1（单步）** 的学生，在保持/提升**文字可读性**的前提下做实时超分，并形成一篇论文。

- **学生/教师骨干**：LightningDiT（0.5B = d1024/b28/h16 + SD2 f8c4 VAE；1.4B = d1536/b36/h24 + Qwen-Image f8c16 VAE）。
- **核心方法线（消融阶梯）**：在一步蒸馏基线上叠加「文字感知」监督/先验：
  - **2a**：局部裁剪 OCR-REPA（用 OCR recognizer 特征对齐，在线 DBNet 检测框）→ **当前识别冠军**。
  - **2a-GT**：同 2a 但用数据集 GT 框（`restoration_dataset.json` 预索引）——检测器 vs GT 的干净消融。
  - **2b**：GT-box CRNN 伪标签 CTC（无突破）。
  - **2c**：detection-map 监督。
  - **2d**：真实 GT 文本 CTC → **检测冠军**。
  - **2a+2d 联合**：识别/检测双新高（两个互补冠军）。
  - **E1**：OCR 语义条件注入（输入侧先验，DINO 式 cross-attn，推理时保留）。
  - **E2**：E1 + 2a-GT 联合（**当前在 199 训练中**）。
- **评测（双协议，缺一不可）**：
  - **协议①** PP-OCR + pyiqa：OCR-CharAcc / EM / CER + PSNR/SSIM/LPIPS/DISTS/NIQE/MUSIQ/MANIQA。
  - **协议②** TESTR 官方 spotting（TAIR 论文协议）：Det F1 / E2E-None / E2E-Full。
  - **铁律**：E2E 必须与 Det F1 一起读（低检测方法会出现虚高的 E2E 伪影）。
- **基准**：Real-Text 847 对（已逐字节核对 = TAIR arXiv 2506.09993 公开测试集，可直接引用论文表）。训练集 119,495 个 `sa_*` 裁剪与其零重叠；SA-Text-test 因母图污染弃用。

---

## 2. 文档索引（按用途）

### 📝 论文写作工作区（AAAI）
| 文档 | 内容 |
|---|---|
| **`paper/AGENT.md`** | ⭐ **论文工作区入口**：写作主线 / 文献定位 / 实验素材 / 参考 PDF。写论文先读这里。 |

### 📐 论文思路 / 方法设计
| 文档 | 内容 |
|---|---|
| `doc/text_distill_master_plan.md` | 实验主计划（阶梯/路线图）。 |
| `doc/text_ocr_local_repa_experiment.md`、`doc/text_distill_2a_breakthrough_analysis.md` | **2a** 局部 OCR-REPA 的机制与突破分析（最深）。 |

> 注：早期规划/设计/命令类文档（项目说明、distillation_summary、ablation_design、ocr_cond_injection_plan、fd_loss、vae_lora 等共 13 篇）于 2026-06-24 清理（内容已被 `result_exp.md` 收编，git 历史可恢复）。

### 🧪 实验进展 / 结果（**单一事实来源**）
| 文档 | 内容 |
|---|---|
| **`doc/result_exp.md`** | ⭐ **实验总记录**：每个实验的动机/配置/机制/训练设置/结果 + 统一对比总表（§0 评测协议、§2 各实验、§3 总表与结论）。**每做完一个实验必须更新此文**。 |
| `doc/realce_benchmark.md`、`doc/2a_ocr_repa_figure_prompt.md` | RealCE 基准 / 论文配图提示。 |

> 评测/推理的**具体命令 + 环境 + 代码检查**集中在 `doc/compute_resources.md` §9（命令集类旧文档已并入那里）。

### 🖥️ 计算资源（服务器/数据/路径）
| 文档 | 内容 |
|---|---|
| **`doc/compute_resources.md`** | ⭐ **算力总览**：3 台服务器的地址/登录/GPU/代码仓/数据/当前占用 + 通用铁律 + E2→199 迁移模板。 |
| `doc/node199_拓扑与配置原理.md` | 199 的 NVLink 拓扑与多卡（有效 batch 16）配置原理与经验。 |

---

## 3. 服务器使用原则（硬约束，必读）

> 本项目运行在 **3 台多人共享服务器**上，资源纪律是第一原则。完整细节见 `doc/compute_resources.md` §0。

1. **绝不杀任何别人的进程。** 只能停自己启动的 screen/进程；缺显存就**等**，不抢不杀。
2. **数据/产物只写各机指定目录**：本地 `/data/ywk/`、199 `/data2/wyw/ywk/`、226.31 `/data07/dt_data/ywk/`（其他盘视为满/只读）。
3. **不改远端 conda 环境**（不装包、不改依赖）。
4. **长任务用 `screen`，不用 `nohup`**；启动器带重试循环。
5. **远端无外网**：HF 用 `HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1`；dinov2 用本地缓存仓 `source='local'`。
6. **远程操作助手**：199 用 `/data/ywk/claude_tools/{rexec,rput,rget}.py`；226.31 用 `{rexec_dt,rput_dt,rget_dt}.py`。
7. 三台机的代码会**分叉**（本地是 OCR-REPA/OCR-cond 超集；226.31 base main 无该栈；199 VOSR_textsr 有 2a 栈）。迁移要 diff + 备份，不盲目覆盖。
8. **可复现铁律 —— 每个实验/操作必须留痕。** 任何一次训练、推理、评测、代码改动，其**具体参数与代码配置都必须记录**，做到只看记录即可复现：① 关键超参（init/teacher ckpt、LR、步数、batch/accum、w/loss 权重、分辨率等）、② 涉及的 config / 数据 txt / 代码改动文件与改动点、③ 机器 / GPU / screen / 启动脚本、④ 结果或状态 + 详情链接。**记录落在两处**：实验级动机/配置/结果入 `doc/result_exp.md`（单一事实来源）；逐次操作流水入下方 **§5 Agent 操作记录表**。**先记录、后执行**（至少同一轮内补全），不允许"跑完了但没人知道怎么跑的"。

## 4. 当前状态（2026-06-21）

- **199**：E2（E1+2a-GT 联合）训练中，screen `e2_199`，GPU1-4，20000 步 ~1.5 天。
- **226.31**：1.4B teacher 已停于 73k（已收敛）、ckpt-70000 质量测试通过（PSNR 23.40/SSIM 0.905，非欠拟合）；**1.4B 一步蒸馏训练中**，screen `distill_1p4b`，GPU1-4，20000 步 ~14h。
- **本地**：主开发 / 评测。

> 维护约定：服务器占用、实验结论变化时，请同步更新 `doc/compute_resources.md` 与 `doc/result_exp.md`，并在此 §4 更新一句话状态。

---

## 5. Agent 操作记录表（逐次操作流水，必填）

> 用途：任何 AI/人对本项目做的每一次**训练 / 推理 / 评测 / 代码改动 / 数据构建 / 迁移**，在此**追加一行**（最新在最上）。目的是可追溯、可复现、可交接。**实验级的动机/机制/完整结果仍以 `doc/result_exp.md` 为单一事实来源，本表是操作流水 + 指针。**
>
> 列含义：**日期** | **操作**（一句话） | **关键参数/配置**（超参、init/teacher、LR/步数/权重等） | **代码/config/脚本**（涉及文件 + 改动点） | **机器/GPU/screen** | **结果/状态 + 详情链接**

| 日期 | 操作 | 关键参数/配置 | 代码/config/脚本 | 机器/GPU/screen | 结果/状态 + 详情 |
|---|---|---|---|---|---|
| 2026-06-27 | **★★★ 1.4B band sweep 完成（两阶段：先全推理→再 metric sweep；ckpt 12000-16000）** | 训练在 ~16400 被第三方挤 GPU4 OOM 中断(停 ckpt-16000=计划80%)；对 12000-16000 五点 NFE=1 qwen 推理 + 协议①(OCR_CHARACC=1−CER+pyiqa)+协议②(TESTR)，用户要求推理与metric分阶段 | 本机编排 `sweep_1p4b_phased.sh`；199 `infer_1p4b_worker.sh`/`metric_1p4b_worker.sh`；结果 `sweep_1p4b_results.log` | 199 GPU1-3(推理+metric) + 本机(TESTR) | ✅ **1.4B 均值 CharAcc 68.70/PSNR 25.52/det 78.57/E2E None 41.14/Full 53.30**。vs 0.5B 冠军(68.74–69.02/24.67/78.32/42.59/52.71)：**文字指标打平、PSNR +0.85、12000-16000 全平台**→16000 有代表性、不续训。**结论:规模买 PSNR 不买文字精度(文字受 OCR 监督上限封顶)→ 正面支撑"贡献在 OCR loss 非规模"**。详 result_exp.md 1.4B节 |
| 2026-06-28 | eval：4baseline 两spotter+协议① 全跑 + SA-Text三级 + 对标TAIR判断 + Real-CE/SAText协议①启动 ✅ | Real-Text847: TESTR+ABCNetv2(det/None/Full)+协议①(CharAcc) 4baseline齐;SA-Text三级两spotter齐(HQ校准对上TAIR±1~3);**判断:输入同128LQ、检测对齐、E2E系统性低~10~16(含TeReDiff自身−16)=TAIR published不可复现非我bug→自评为准**;TeReDiff Real-CE(PRISM)ACC34%≈我们(全图共性);ABCNet 4补丁(THC/Beziers__len__/postproc cpu/pairs.get) | `eval/protocol/{l1_abcnetv2.py,run_l1_testr.sh,run_satext_l1.sh,run_satext_proto1.sh}`;结果 `eval/results/`;台账 result_exp.md §2「UTEP统一baseline对比」 | 本机GPU0-3 + 199(协议①) | 写入 result_exp.md(a-e);Real-CE baseline metric(f)+SAText协议① 跑中待回填 |
| 2026-06-27 | eval：FaithDiff env+权重+smoke ✅ + ABCNet v2 第二spotter 搭好 ✅ | FaithDiff(faithdiff env,torch2.4+cu121,hub0.25.2;SDXL仅fp16删fp32省10G;test_wo_llava 需中性caption json)smoke过→847跑中(GPU0)。ABCNet v2=克隆tair env→AdelaiDet编译(ml_nms.cu THC→at::ceil_div/cudaMalloc/AT_CUDA_CHECK补丁)→`import adet._C`全局可用+totaltext权重 | `eval/repos/{faithdiff,abcnetv2}`；补丁 `abcnetv2/adet/layers/csrc/ml_nms/ml_nms.cu` | 本机 GPU0 | 4 baseline env 全通；ABCNet v2 待写评测脚本。详 `eval/SETUP.md` |
| 2026-06-27 | eval Part B：3/4 baseline 全量847 SR 完成 ✅ | OSEDiff/SeeSR/DiffBIR 各 847/847 SR(512)；FaithDiff env✅(修 hub0.25.2)+权重下载中(RealVisXL SDXL~6.5G/VAE/FaithDiff.bin) | `eval/outputs/{osediff,seesr,diffbir}/realtext847`；outputs 共~872M | 本机 GPU0/1 | 待 FaithDiff 权重→smoke→847；之后写 UTEP 评测脚本 |
| 2026-06-27 | eval Part B：DiffBIR env建好+smoke+全量847启动 ✅ | 新建 `diffbir` env(7.4G,torch2.2.2+cu118)；坑：①SD2.1 5G ckpt 改软链复用 mymodels ②`setuptools<81`(pkg_resources)；v2 task captioner none 免LLaVA；smoke 5图✅,847 后台(GPU1) | `eval/repos/diffbir/weights/`(v2.pth下载,SD2.1软链)；out `eval/outputs/diffbir/realtext847` | 本机 GPU1 | smoke✅,847🔄。下一步 FaithDiff env |
| 2026-06-27 | eval Part B：OSEDiff 全量847 + SeeSR env修复+smoke+全量847（AB并行）✅ | OSEDiff 847/847 SR(245MB,NFE=1,GPU0完成)；SeeSR env 修补一串依赖(hub0.25.2/opencv/torchvision0.15.2+cu117/einops/pl/scipy/timm…)后 smoke 通过,847 后台跑(GPU0,50步,输出 sample00/) | `eval/outputs/{osediff,seesr}/realtext847`；seesr 原版 bert 路径干净 | 本机 GPU0/1 | OSEDiff✅完成；SeeSR🔄847跑中。下一步 DiffBIR/FaithDiff env |
| 2026-06-27 | OSEDiff smoke 通过（eval Part B 首条链路）✅ | 5图 128→512 SR,NFE=1,GPU0；env `osediff_fix`；统一输入定为 **PNG**(各仓 glob 不一,OSEDiff 只认 png)；HF cache=`/data/ywk/.cache/huggingface` | 补丁 `eval/repos/osediff/ram/models/utils.py`(bert 路径改回 `'bert-base-uncased'`)；out `eval/outputs/osediff/_smoke5` | 本机 GPU0 | ✅ 通过。下一步：转 847 LR→png、OSEDiff 全量；建 DiffBIR/FaithDiff env；修 seesr env |
| 2026-06-27 | 建统一评测框架 `/data/ywk/eval` 骨架 + **原版 git 克隆**核心4 baseline（ToDoExp/05 Part A）✅ | 4 仓全为原版新克隆(本机旧 SeeSR/OSEDiff 代码已改坏不可复用,**仅权重软链复用**)；OSEDiff 仓自带 osediff.pkl+DAPE，SeeSR 权重软链自 `/data/ywk/SeeSR/preset/models`，RAM/SD2.1 复用在盘 | SeeSR/OSEDiff/DiffBIR(XPixelGroup)/FaithDiff(JyChen9811)；env：osediff_fix✅、seesr(需修hub0.25.2)、diffbir/faithdiff 待建 | 本机（clone+软链权重） | ✅ 完成。映射写 `/data/ywk/eval/SETUP.md`。下一步：修SeeSR env→SeeSR/OSEDiff smoke；建 DiffBIR/FaithDiff env+权重 |
| 2026-06-27 | **★★★ 破0.30 收割：一步学生+文字tile+2a OCR-REPA(ckpt-8000)评 Real-CE** | 训练跑满8000步(10h28m,loss0.261,ocr_repa0.0222收敛);4卡分片一步推理 tile512/ov64 → 合并 → `eval_realce.py`(官方CRNN同口径) | 收割 `claude_tools/harvest_realce_2a_tiles.sh`(用全路径 `envs/vosr/bin/python` 避 conda activate 回退 base 缺 cv2);结果 `teacher_eval/REALCE2A_realce_eval.json` | 本机 GPU0-3 screen `realce2aeval` | ✅ **rec_acc 0.3012 破0.30**(>gtsup 0.2991,距 RRDB 0.3093 仅 0.008);PSNR17.79 仍低(范式天花板)。覆盖257/260(3张超大图单卡亦OOM排除)。详 result_exp.md Real-CE节「★★★破0.30」 |
| 2026-06-27 | **挂 1.4B 收尾 sweep 守护脚本（双协议，复刻 0.5B 噪声带口径）** | 等 ckpt-20000 落盘→对 16000-20000 共5个ckpt：协议①(199 osediff，inference NFE=1 qwen + PP-OCR/pyiqa) + 协议②(本机 tair，拉SR + TESTR det/E2E None+Full)；串行 199 GPU1（礼让，训练结束即放 GPU2-4） | 本机 orchestrator `watch_1p4b_sweep.sh`（轮询+驱动两机，短 rexec）；199 worker `sweep_1p4b_worker.sh`（克隆 `metric_realctc.py`→swap `1p4b_<step>`/`qwen` stem、GT=real_test/HQ）；结果 `sweep_1p4b_results.log` | 本机 screen `watch1p4b` + 199 `sw_<step>` | ✅ 已挂、轮询中（读到 15236/20000）。到 20000(~16h后)自动触发。**坑规避**：inference 自动读 exp/args.json 取 1536/qwen 无需 --config；SR stem 是 `qwen_steps1_seed42_shortcut`(非 sd2)。对照 0.5B 噪声带 CharAcc 68.74–69.02/det 78.32。详 result_exp.md 1.4B节 |
| 2026-06-26 | **★小代价破0.30:一步学生+文字tile+2a OCR-REPA(纯GT监督)** | 诊断后转向:一步/低漂移+文字感知才是Real-CE对的载体(非多步teacher)。gtsup0.2991升级版:init=2a+2d冠军ckpt-18000、**纯GT监督teacher=None**(避漂移,gtsup最优)、**我们的文字聚焦tile(4821)+真实13mm对**(替代645随机裁)、**2a OCR-REPA(中文安全,gtsup当年无OCR监督)**、跳过2d(CTC中文字符集风险)、LR5e-5、8000步、eff16。建`realce_tile_boxes_index.pkl`(4821tile/15043框,tile坐标) | config `VOSR_0.5B_realce_2a_tiles.yml`+`realce_tiles_only_dataset.txt`+`make_realce_tiles.py`/index;`run_realce_2a_tiles.sh` | 本机GPU0-3 screen `realce2a` | 🔄 训练中~5.3s/it loss0.2-0.6,ETA~12h。冒烟:载入冠军+tile index4821+teacher=None+2a激活✓。到8000(或中途ckpt)评Real-CE看能否>0.2991/破RRDB0.3093 |
| 2026-06-24 | **★★ 1.4B batch A/B 实测 + 定 per-device（方法论坑：on_sync OCR 权重 ∝ 1/accum）** | A=per-device1/accum4/w0.5 vs B=per-device2/accum2/w0.5（均 effbs16、4卡）；测瞬时 s/it + 峰值显存 | 临时 `VOSR_1.4B_ab_pd2.yml`/`run_ab_pd2.sh`（测完已删）；查码 `train_vosr_distill.py:1363` `accelerator.accumulate`÷accum + OCR `on_sync_only` | 199 GPU1-4 / screen `ab_pd2`(测)→`distill_1p4b`(A) | **A 12.5s/it~46GB；B 10.0s/it~50GB(快20%,无OOM)**。但 OCR `on_sync_only`→**有效权重=w/accum**：A=0.125(=0.5B冠军)、B=0.25(×2偏离)。**用户选 A**(零可比性风险,弃20%提速)。**通用教训:改 accum 会静默改 on_sync aux 权重,调 batch 必同步核 aux 权重**。详 result_exp.md 1.4B节 |
| 2026-06-24 | **★ 1.4B 正确 shortcut 蒸馏启动（199，复刻 0.5B 2a+2d 配方）** | teacher=pretrained=**1.4B fm ms_from_pretrained ckpt-40000 EMA**（CharAcc 65.63，226→本机→199 迁移 5.2G）、distill_type **shortcut**、dim1536/depth36/h24、**ae qwen f8c16**、enc **dinov2l/z1024/layer17**（配 teacher）、2a(ocr_repa 0.5)+2d(ocr_real_ctc 0.5) GT-box、LR 5e-6、20000步、ckpt/1000、effbs16（bs2×accum8，per-device1）。**只迁权重+box index，未搬 226 代码**（199 `train_vosr_distill.py` 已支持 qwen+2a/2d，纯 config 适配） | config `VOSR_1.4B_codsr_2a2d.yml`（派生自 0.5B `..._ocr_repa_realctc`，换 1536/qwen/dinov2l/teacher/sa_remote 路径）；`run_1p4b_2a2d_199.sh`（torchrun nproc2、NCCL_P2P_LEVEL=NVL、retry+auto-resume）；迁移 `rget_dt`→`rput` teacher ema + `text_boxes_index.pkl` | 199 GPU**1,2,3,4**（GPU4 与他用户共租，66GB free）/ screen `distill_1p4b` | ✅ 训练中、**effbs16(bs016，bs4×accum4，per-device1)**、~13s/it、v_loss~5e-4、4 卡各占~46GB（GPU4 含他人15GB=59GB）、ETA~3天。teacher 载入 missing keys=6（shortcut/aux-time，符合预期）。**坑1:此 codebase `train_batch_size` 按 GPU 数均分→effbs=bs×accum（非×GPU），且 bs 必须≥GPU 数（bs2 on 4 卡=2//4=0 崩）→4 卡用 bs4×accum4。坑2:首启 bs2×accum4=effbs8 偏小→已对齐 effbs16**。**★ 重要测量:4 卡(1,2,3,4) ~13s/it ≈ 2 卡(2,3) ~13s/it，提速≈0——固定 effbs16 下每 opt 步瓶颈是 sync 步的全 512 qwen OCR-decode(interval1，每步一次)+GPU1 跨 NUMA 的 1.4B 梯度 all-reduce，非微批并行；加卡在此结构下不增吞吐。用户拍板保持 4 卡。真提速需 OCR interval=2(~2×，但偏离 0.5B 配方)**。详 result_exp.md 1.4B节 |
| 2026-06-24 | **2a+2d 噪声带 sweep（最后5个ckpt 16000-20000，双协议）** | 4 ckpt 并行（本机GPU0-3 各一），inference NFE=1 → 协议②TESTR(本机) + 协议①PP-OCR(199 GPU2 4并发) | `sweep_2a2d_worker.sh`(STEP/GPU 参数化)；`sweep_2a2d_000NN.log`；199 `metric_2a2d_000NN.{py,log}` | 本机 GPU0-3 sweep_* + 199 GPU2 m_2a2d_* | ✅ **CharAcc紧带68.74–69.02**(mean 68.82,峰 ckpt-18000=69.02)；**20000非全表最优**(det/PSNR最高但E2E/CharAcc带底)，**18000综合最优**(CharAcc+E2E Full+det次高)。论文报均值68.8或峰18000。详 result_exp.md 2a+2d噪声带 |
| 2026-06-24 | **ToDoExp/03 Part0：hybrid 转录脚本跑通 + 启动全量645** | hybrid=renov行级框→按行裁crop(任意宽高比,因29% Real-CE行宽>512px,管道512方crop会丢长行)→双VLM(Ovis2-8B+Qwen2.5-VL-7B)两阶段转录(复用vlm_processing helper)→**逐行取一致**(非管道逐图all-or-nothing)→dataset.json(原图坐标poly+text+agree)；**坑1 flash_attn2在torch2.5报`torch.library.wrap_triton`缺失→Qwen改`attn_implementation=sdpa`**;**坑2 HF xet后端卡死→`HF_HUB_DISABLE_XET=1`**;**坑3 home满→所有cache(HF/torch/pip)迁/清到`/data/ywk`,清pip31G**;padding扫:0.06(76.1%)>0.12(69.3%,邻字带入更差)→定0.06 | `dataset_curation/hybrid_transcribe_realce.py`、`run_hybrid_full.sh`；权重缓存 `/data/ywk/hf_cache`(HF_HOME) | 本机 GPU0 screen `hybrid_full` | ✅ **完成(1.7h)**:`dataset_realce_train.json`(3.7M)=**9037行→6905一致(76.4%)**,608/645图有标注(缺37张=renov框文件size=0的TextSR-20211222系列,可后补det_annos),一致行72%含中文(4971行),中位7字/行。样例质量高("【适应症】荨麻疹、过敏性鼻炎、湿疹、"等) |
| 2026-06-26 | **★ Real-CE eval 诊断:tiling 伪影控制实验（质疑PSNR17<输入红旗）** | 用户质疑:PSNR17<输入19.65像eval伪影非模型坏。控制=12个文字区512crop,**非tiled**,VOSR_0.5B_ms(论文权重)+我们teacher(40k),cfg0.5/steps25。文献核实:合成+真实混训是标准(TADiSR/TIGER);TADiSR仅337 Real-CE就超RRDB(数据量非主因);跨论文协议不可比(0.882vs64.7%) | `tiling_ctrl/`(建crop+infer+psnr+ctrl_visual.jpg) | 本机 GPU0-3 screen `ctrl_*`/`ov_*`/`bs_*` | ✅ **诊断完成(tiling清白,纠正过早结论)**:**①overlap64→256重评:rec_acc 0.2894→0.2867/PSNR17.23→17.26 几乎不变→tiling缝非问题;②同一文字区512同模型 tiled vs 非tiled PSNR 18.97 vs 19.11=仅+0.14dB→tiling清白**(先前"2dB"是口径错:文字区PSNR19.11 vs 全图PSNR17.17范围不同)。**全图PSNR17低是背景拉低非tiling**。**真因=范式**:RRDB(确定性CNN)PSNR20.14>输入19.65/rec0.3093 vs 我们扩散25步生成式漂移PSNR≤输入/rec0.28-0.29;**base VOSR论文权重同样漂移→非我们训练问题**。**线索:步数越少漂移越小→一步学生(gtsup0.2991/2aGT0.282)>25步teacher(0.2741-0.2894),TADiSR也单步t=200才超RRDB→Real-CE温和真实退化宜少步/低漂移复原,多步生成teacher是错工具**。→base VOSR全260 rec_acc基线跑中(cfg2.0,screen `bs_*`) | **★base VOSR论文权重(通用,cfg2.0)=rec_acc 0.2459/PSNR17.39/LPIPS0.347最差→幻觉文字、<输入0.275**。坐实:①文字特化有用(base0.2459<我们文字模型0.27-0.30);②高cfg漂移更重;③**全景:base0.2459<输入0.275<teacher0.2741<2aGT0.282<mix0.2894<gtsup0.2991<RRDB0.3093**。**结论:一步学生gtsup0.2991仅差RRDB0.010,多步teacher混训是错工具(漂移),正确方向=一步/低cfg学生侧+文字聚焦tile+2a/2d OCR监督→很可能追平RRDB,且一步~50×快+主SA训练(跨域强story)** | 
| 2026-06-25 | **★ teacher SA+Real-CE 正经混训启动（用我们管线的文字聚焦tile，处理量差）** | 用户纠偏:de-risk的645是整图(teacher随机裁512多落背景),应**用我们管线的框裁文字聚焦tile**。`make_realce_tiles.py`:filtered 5640框→**4821个512文字tile对**(HR52/LR13同坐标,IoU0.7去重);量差处理=Real-CE tile **上采样×25**(4821×25≈12万≈SA 11.9万,批次~50/50)。**代码手术(混批):** `realsr_dataset.py`teacher分支改**按样本**(真实LR存在→真实+has_real_lq=1,否则零占位+0,collate统一);`train_vosr.py`GPU环`torch.where(flag,real_lq,syn_lq)`。init=teacher ckpt-40000、LR1e-5、20000步、eff8 | config `VOSR_0.5B_text_hr_realce_mix.yml`+`realce_mix_dataset.txt`+`make_realce_tiles.py`+`run_realce_mix.sh`;patch realsr_dataset.py/train_vosr.py | 本机 GPU0-3 screen `realce_mix`+`mxev_*` | ✅ **结论:混训有效但无突破**。训练20000步10.5h loss0.689→0.514。**mix teacher Real-CE: rec_acc 0.2894(我们teacher最好,0.2741→0.2812→0.2894每步+0.008仍升)/PSNR17.23/SSIM0.630/LPIPS0.261(最低,幻觉最少)**;但仍落0.28~0.30簇、**≈纯SA训练2a-GT学生零样本0.282-0.284(且mix teacher评在少12张的更小子集n=249不可靠)**、仍<RRDB0.3093。**铁律:无论teacher/学生注入、合成/真实退化、多少步,所有Real-CE全卡0.28~0.30无人破RRDB→瓶颈=真实数据量(645图/4821tile)+真实退化中文SR难度,非方法/注入点。坐实跨域一致性定位+C2(可读性增益来自学生侧OCR非teacher域)**。847防遗忘暂堵(199全占+本地无paddleocr/847HR)。详 result_exp.md | 
| 2026-06-25 | **teacher 混训 de-risk:纯 Real-CE 真实对续训多步 teacher（有界尝试第一步）** | 用户拍板 teacher 混训(有界)。先最低成本验证"多步 teacher 能否学真实域":init=teacher ckpt-40000 ema续训、纯 Real-CE 13mm→52mm 真实对(645)、LR 2e-5、4000步、eff8、本机4卡。**patch `train_vosr.py:760` 用 `batch["lq"]`(真实对)当存在否则合成(向后兼容)**;dataset 走 realsr `real_paired_lq_dir`+`_apply_same_crop`。**注:真full混批(SA合成+Real-CE真实)受限于 real_paired 是dataset级全局开关+filter_collate混批不兼容,故de-risk先纯Real-CE(会遗忘SA但只为答"teacher能否学真实域");动了再做防遗忘混批手术** | config `VOSR_0.5B_text_hr_realce_ft.yml`、`run_realce_teacher_ft.sh`、patch train_vosr.py | 本机 GPU0-3 screen `realce_tft`+`ftev_*` | ✅ **de-risk结论:方向"活但小+数据受限"**。训练 loss 1.33→0.33。**FT teacher Real-CE: rec_acc 0.2741→0.2812 / PSNR 17.17→17.34 / SSIM 0.603→0.623 / LPIPS 0.298→0.279 / mask_psnr 9.99→10.14 —— 6指标一致上移(真·学到,主要少幻觉)但幅度极小(rec +0.007,文字区mask_psnr仍~10基本没重建)**。横向铁证:所有Real-CE尝试 realpair0.2957/gtsup0.2991/FTteacher0.2812 全卡0.28~0.30,**无人破RRDB0.3093,且不分注入点(teacher/学生)→瓶颈是数据量(645图)非方法/注入点**。FT teacher 0.2812还<学生gtsup0.2991。**结论:不追Real-CE SOTA,锁跨域一致性story(story_evidence_audit本就如此定位)**。详 result_exp.md |
| 2026-06-25 | **评 teacher 自身 Real-CE 质量（teacher混训决策闸门）** | 动机:用户洞见——学生阶段Real-CE微调我们已3次null(realpair0.2957/gtsup0.2991<RRDB0.3093),因一步学生被钉在teacher合成域;故先量teacher(多步)自身Real-CE,若teacher都上不去则混训必要性+预期明朗。0.5B text teacher ckpt-40000、多步25步、**cfg0.5**(config值)、wc0.1、u1 tiled512/overlap64、val 13mm→SR、260 valid_list分4片4卡 | `inference_vosr.py`(多步)+`scripts/eval_realce.py`(官方CRNN ACC/NED+PSNR/SSIM);`teacher_eval/harvest_teacher_eval.sh` | 本机 GPU0-3 screen `teval_0..3`+`teval_harvest` | ✅ **闸门结果(决定性):teacher自身Real-CE rec_acc 0.2741/PSNR 17.17/SSIM 0.603/mask_psnr 9.99(249/260图)** —— **≈输入13mm 0.275、< RRDB 0.3093、< 我们蒸馏学生2a-GT 0.2935、PSNR还低于输入19.65**。渲染证实:大号清晰字teacher≈输入(轻锐化),小字/密集字幻觉糊化→生成式teacher在真实退化域外乱生成(TAIR text hallucination)。**含义:域gap在teacher层(非仅学生锚定)→学生阶段微调注定null已被坐实(realpair/gtsup 3次null一致);teacher混训是唯一技术杠杆且合领域做法,但payoff不确定(645图少+gtsup纯监督也只0.2991+teacher须学生成式真实SR)**。坑:①valid_list CRLF→\r匹配0图(tr -d修);②inference_vosr.py默认--cfg_scale 2.0传进模型→须显式--cfg_scale 0.5。详 result_exp.md |
| 2026-06-24 | **ToDoExp/03 Part0:13mm(LR)-52mm(HR)配对+配准过滤** | 13/52同尺寸像素级配准(ratio1.000)→52坐标框直接共用;过滤=逐一致行算 NCC(GaussBlur(HR),LR)+phaseCorr shift,**保留 NCC≥0.3 & shift≤15px**(标定:抓错位"会展中心shift121"/LR文字糊没"10号线",留模糊但对齐的真LR-HR);误伤少数"很糊+轻偏移"边界对(保守高精度) | `dataset_curation/filter_pairs_realce.py`;输出 `dataset_realce_train_paired_filtered.json`(每条:image+hr/lr路径+poly+bbox+text+ncc+shift) | 本机(CPU) | ✅ **用户定阈值 NCC≥0.25**:6905一致行→**5640保留(81.7%),558图**(阈值不敏感:0.2↔0.35仅差6%)。`dataset_realce_train_paired_filtered.json`(每条:image+hr/lr绝对路径+poly+bbox+text+ncc+shift)。保留率远宽于TADiSR ~17%。**停此,等用户确认再转train index格式(仿text_boxes_index/restoration_dataset.json)+混入蒸馏训练(Part A)** |
| 2026-06-24 | **ToDoExp/03 Part0：本机建 `dataset_curation` env + 验证中文检测召回（关键负结果）** | env=py3.10/torch2.5.0+cu124/transformers4.51.3/flash-attn2.8.3(prebuilt wheel,避cross-device坑)/detectron2+adet build develop(setuptools59.5.0,CUDA_HOME=/usr/local/cuda-12.4)；权重 Bridge_tt.pth(330M,drive 15GnzJx,TotalText DG-Bridge)；**DiG/checkpoint-9.pth 用 stub `{"model":{}}`**(strict=False加载后被Bridge_tt的305 recognizer键覆盖,inference等价)；验证=8张 Real-CE 52mm 跑 start→cropping→bridge_stage2(NFE检测,GPU0) | 新建env `dataset_curation`(不动现有env)；`dataset_curation/config_realce.yaml`(指向 train/52mm)；`viz_detections.py`/viz脚本；输出 `/data/ywk/datasets/Real-CE/curation/realce_sample8/` | 本机 GPU0(检测<30s) | ⚠️ **env全通(detectron2/_C+adet+flash_attn+VLM类均import OK)；但 Bridge(TotalText英文spotter)对中文近零召回**:8图仅59框全落英文/数字/大字标题,_16整屏中文段=0框,_12(KFC)5行中文正文=0框,_0上半10+行中文=0框只框底部英文块。**纯管道不可用**。hybrid需 Real-CE train 行级框→本机train无annos。**用户拍板走hybrid**。**199 `/data2/wyw/ywk/datastes/Real-CE/train/` 有全套train标注**:det_annos/trans_annos_52mm/_renov 各645全覆盖,原图像素坐标;转录是英文模式OCR垃圾("der182008")但**框是PP-OCR中文检测,召回好**(已拉本机+overlay验证:_16 Bridge=0→det14框全覆盖中文;_12 renov 8框=行级正文全覆盖)。→hybrid=用renov行级框裁crop+丢弃垃圾转录+双VLM(Ovis+Qwen)重转录。已拉 det_annos/trans_annos_52mm/_renov 到 `/data/ywk/datasets/Real-CE/train/`。下一步:建 GT框→pipeline vlm阶段 适配器→`--start_from vlm1_recognition`→dataset.json |
| 2026-06-24 | **1.4B teacher(ms_from_pretrained)训练完成 + 启动 847 评测推理** | 训练 40000/40000 干净退出(loss~0.15)、init=VOSR_1.4B_ms、LR 1e-5；评测推理=25步 cfg0.5 wc0.1 Qwen-VAE | `inference_vosr.py`(非onestep)、`run_teacher40k_847.sh`(派生自 run_teacher_ms.sh) | 226 GPU1 `teacher40k` → 自动链 `harvest_teacher40k` → 199 GPU2 metric | ✅ **完成:CharAcc 65.63**/PSNR24.78/SSIM0.786(全847)。**from-pretrained 修好识别欠拟合**:36.82→65.63(+28.8),超目标0.5B teacher 63.78。**"1.4B不成"定论=从零init配置疏漏,非规模/VAE/数据问题**。下一步可选:正确 shortcut 蒸馏(train_vosr_distill.py+2a/2d)看1.4B学生能否超0.5B 68.8。详 result_exp.md 1.4B节 |
| 2026-06-24 | **gtsup(Real-CE论文式监督)收割链启动** | 199 GPU2:Real-CE val(13mm→SR,tiled)+847推理(u4,NFE1)+847协议①PP-OCR metric;本机GPU1:拉SR→eval_realce(Real-CE)+847协议②TESTR | `gtsup_infer_199.sh`(stage1)、`harvest_gtsup_local.sh`(stage2,轮询STAGE1 DONE自动拉取) | 199 GPU2 `gtsup_harvest` + 本机 `harvest_gtsup_local` | ✅ **收割完**:Real-CE PSNR **18.00**/rec_acc **0.2991**(我们Real-CE微调最优,>真实配对0.2957,但<RRDB 20.14/0.3093);847②det 73.34/E2E Full 57.58;①CharAcc **67.34**/PSNR25.15(无遗忘,≈2a)。**坑:screen内`conda activate`静默回退base(缺cv2/paddleocr)→metric先崩→改全路径python修复**。详 result_exp.md gtsup节 |
| 2026-06-23 | **Experiment B：结构增强 + 2a+2d 联合蒸馏**（本机4卡） | teacher=结构增强多步 ragp_lq_mod_vae_lora ckpt-20000、repa_type ocr(2a) repa_weight0.5 + ocr_real_ctc_weight0.5(2d) + use_lq_token_modulation、bs4、use_checkpoint True | config `VOSR_0.5B_codsr_2a2d.yml`；`run_codsr_2a2d.sh` | 本机 GPU0-3 / screen `codsr_2a2d`→收割 `harvest_codsr` | ✅ **完成+已收割**：①CharAcc **68.45**/PSNR23.61/SSIM0.821/LPIPS0.245/CER0.316 ②det **77.51**/E2E Full **53.57**。**结论：结构增强未叠加**（≈纯2a+2d 68.74/78.32，噪声内，PSNR略低）→我们的OCR loss才是贡献点。详 result_exp.md「结构增强×2a+2d」节 |
| 2026-06-23 | **Real-CE 论文式监督微调（真实退化13mm→52mm，199 GPU1-4）启动** | teacher=None(纯监督)、init=2a remote ckpt-20000、real_paired_lq_dir=Real-CE/train/13mm(645图真实LR)、LR 5e-5、10000步、eff-batch16、ocr_repa_weight 0.0(纯recon,无OCR aux)、use_checkpoint False | **修复**`vosr.py:_teacher_target` GT监督分支→`v=v_target`(直接回归GT速度，删掉那次多余 model(inp_weak) 前向→既消除RNG/检查点不一致崩溃，也更忠实论文式纯监督)；`VOSR_0.5B_realce_gtsup.yml`、`run_realce_gtsup.sh` | 199 GPU1-4 / screen `realce_gtsup`(3044175) | ✅ 训练中 18/10000、v_loss 0.81↓(1.34→0.53→0.81)。**bug已解**(原"等A/B"作废)。坑：远端 sed 静默失败→拉-改-推同步 vosr.py。到10000收 Real-CE+847+TAIR 看真实退化纯监督能否破 null |
| 2026-06-22 | **Real-CE 论文式监督微调**（GT 监督+真实退化对，非合成/非 teacher 蒸馏） | **teacher_ckpt:~ → GT 监督**（去掉合成域 teacher pull，疑 null 元凶之一）+ real_paired 真实 13mm→52mm + 保留 ocr_repa(2a) + init=2a-GT、LR5e-5、bs3、10k 步 | **patch `train_vosr_distill.py`**：teacher_ckpt 空时 model_tea=None（GT 监督，`.bak_pre_gtsup`）；config `VOSR_0.5B_realce_gtsup.yml`；`run_realce_gtsup.sh` | 199 GPU1-3 / screen `realce_gtsup` | ✅ "GT-supervised mode" 生效、0/10000 训练中。到 10k 收 Real-CE eval 对比 null 0.2935 |
| 2026-06-22 | E1 代码核验 + 停训（注入式 OCR 无效定论） | extract 函数=distill 版逐字、OCR 层全训练了→非 bug | 读 safetensors gate/层 | 199（已停） | gate −0.013 平台、非 bug、可信 → 已停，199 GPU1-3 释放给 Real-CE。详 `result_exp.md` E1 节 |
| 2026-06-22 | **TeReDiff 完整溯源审计**（回应权重/代码质疑） | 复现权重 md5 `a54ced6d…`=官方 demo（≠重训 `189b84ac…`）；IQA 忠实照搬 `temps/TAIR/val.py`；同图 TESTR 复核 | `iqa_terediff.py`（199 osediff GPU4）+ `eval_spotting.py`（本机 GPU0） | 199 GPU4 + 本机 GPU0 | **IQA 三方一致**（PSNR23.30/LPIPS0.285/DISTS0.238 逐位）+ 识别率 60.07≈我 60.61 + 同图 TESTR det76.14/E2E33.53 → 模型/权重/评测全对，缺口锁死在论文 E2E 49.39 本身。详 `result_exp.md` §3.2 TeReDiff 节 |
| 2026-06-22 | **E1 多步控制实验**（证明 OCR-cond 在 GT 监督下是否有效，反证蒸馏范式废了它） | 多步 `loss_fm`（GT 速度监督，非 OCR-盲 teacher）+ use_ocr_cond；init=VOSR_0.5B_ms、LR1e-5、bs3×accum2、inference_steps1 | **移植 OCR-cond 进 `train_vosr.py`**（helpers+encoder+model kwargs+2处z追加 via `/tmp/patch_ocr_cond.py`）+ 补 dinov2 local 离线补丁（`.bak_pre_ocrcond`）；config `VOSR_0.5B_text_hr_ocr_cond.yml`；`run_e1_multistep.sh` | 199 GPU1-3 / screen `e1_multistep` | ✅ "OCR-cond enabled dim=768"、23/40000、~1s/it、无报错。**看点=gate 是否长成正值**（vs 蒸馏 ≈0）→ 证 OCR-cond 机制有效、蒸馏范式是 blocker |
| 2026-06-22 | **停 E2 训练 + 三协议评测**（用户要求，顺序 metric→TAIR→Real-CE） | 停于 19583（用 ckpt-19000 clean ema）；847 NFE=1 推理（OCR-cond+dinov2 离线 OK） | 停：`pkill -f VOSR_0.5B_text_guided_target_no_rc_ocr_cond_local_gt`；推理 `inference_vosr_onestep.py`（HF offline）；协议① `metric_e2.py`（199 OSEDiff） | 199 GPU1 / screen `e2_infer`+`metric_e2`；协议② SR 已拉本机 `preset/results/e2_847` | E2 已停；847 SR✓；协议① 评测中→②TESTR→③Real-CE，逐个报告。对比 2a-GT |
| 2026-06-22 | **1.4B 蒸馏启动**（从重训 teacher ckpt-70000，simple，NFE=1 学生） | teacher=pretrained=ckpt-70000 clean ema、distill_type **simple**、effbs8（**bs4×accum2，本机修正**：原 simple 是 2GPU 配方 bs2 在 4 卡上 `2//4=0` 崩→改 bs4 local1）、LR 2e-5、20000 步、ckpt/5000、ts2/u_weight1 | config `VOSR_1.4B_distill_t70k.yml`（sed 自 `VOSR_1.4B_sa_text_simple_4gpu_data07.yml`）；脚本 `run_distill_1p4b_t70k.sh`/`chain_distill_after_test.sh`（测试完成自动接力，≥55图安全闸） | 226.31 / GPU1-4 / screen `distill_1p4b` | ✅ 训练中 13/20000、loss 0.15、~2.5s/it、ETA ~14h。**注**：0.5B headline 是 shortcut，此处沿用既有 1.4B simple 管线；如需同口径对比可改 shortcut |
| 2026-06-22 | 1.4B teacher 质量测试（ckpt-70000，判欠拟合） | 25 步多步、cfg0.5、wcs0.1、u4、60 图；vs 本机 real_test/HQ | `test_teacher_70k.sh`；本地 PSNR/SSIM | 226.31 GPU1 推理 + 本机评测 | **PSNR 23.40 / SSIM~0.905**（非欠拟合，上版糊）→ teacher 可用，放行蒸馏。详 `result_exp.md` 1.4B 节 |
| 2026-06-22 | 1.4B teacher：loss 曲线判收敛 + **停训** + 测 ckpt-70000 | loss 自 ~36k 平台 0.15（窗口均值 0.151–0.157）；停于 73k（用户同意，loss 已饱和）；测试=25 步多步推理、cfg0.5、wcs0.1、u4、60 图子集 | 停：`pkill -f VOSR_1.4B_sa_text_ms_bs16_scratch_data07`（仅我的进程）；测：`test_teacher_70k.sh`（ckpt-70000 `clean_weights/ema_model.safetensors`） | 226.31 / GPU1-4 已释放 / screen `test_teacher`(GPU1) | teacher 停止 ✓ GPU1-4 空；60 图测试跑中→拉 199 评测 PSNR/CharAcc 判是否欠拟合（上版 1.4B 欠拟合不可用）。OK 则启 1.4B 蒸馏 |
| 2026-06-21 | Real-CE 真实配对微调（**本机·在线检测版**，与 199 GT-box 互补） | init=本机 2a-GT ckpt-20000、teacher=本机 text 40k、w=0.5、5e-6、5000 步、**在线 DBNet 检测**（本机有 shapely）、real_paired_lq_dir=本机 13mm | config `VOSR_0.5B_realce_2a_realpair_online.yml`；从 199 拉 13mm(327M tar)到本机 train/13mm；脚本 `run_realce_realpair_online_local.sh`/`wait_then_launch_realpair_online_local.sh`(等 4 卡各≥20GB) | 本机 / GPU0-3 / screen `realce_realpair_online`(守护 `wait_realpair_online`) | 冒烟过(645 图/hq≠lq/默认 collate ✓)；**等 GPU0/1/3 空**(别人占用中)→ 自动启动 |
| 2026-06-22 | 1.4B 蒸馏到 20000 → val 收割启动 | distill ckpt-20000、real_test 847、NFE=1、u4 | `val_distill_t70k.sh`（inference_vosr_onestep.py）；exp `exp_vosr_1p4b_distill_t70k` | 226.31 GPU1 / screen `val_1p4b`；协议② 本机 GPU1 `testr_1p4b`；协议① 199 `metric_1p4b` | 847 出图✓。**协议② Det F1 66.95 / E2E None 26.76 < 0.5B（2a 76.6/41.93）→ 1.4B simple 蒸馏不及 0.5B shortcut**；协议① CharAcc 评测中。详 `result_exp.md` 1.4B 节 |
| 2026-06-22 | ★ 查实 1.4B"蒸馏"是 bug：跑成 flow-matching | 用错训练器 `train_vosr.py`（`loss_fm` 纯FM，distill_type 被忽略）；正确器=`train_vosr_distill.py`（`loss_fm_distill_simple`=FM+shortcut自一致，已支持 qwen+teacher） | 读 `vosr.py:141/168`、`train_vosr_distill.py:509/904` | 226.31 | det 66.95 非"1.4B不行"而是"FM模型@NFE1差"；待正确重蒸。详 `result_exp.md` 1.4B 节 |
| 2026-06-22 | ★★★ 深挖 0.5B成/1.4B不成 → 根因=init | 对比 teacher config：0.5B `pretrained_ckpt=VOSR_0.5B_ms`（微调）vs 1.4B `~`（从零）；**`VOSR_1.4B_ms` 预训练权重存在却没用** | 对比 `VOSR_0.5B_text_hr.yml` vs `VOSR_1.4B_sa_text_ms_bs16_scratch_data07.yml` | 226.31 | **1.4B 从零训是配置疏漏**（非模型问题）；fix=从 VOSR_1.4B_ms init。详 `result_exp.md` 1.4B 节 |
| 2026-06-22 | 测 `VOSR_1.4B_ms` 预训练自身 CharAcc | 现成预训练 1.4B、25步、120图 | `test_1p4b_ms.sh` | 226.31 GPU4 / 199 `m1p4bms` | **CharAcc 37.74**（vs 从零训 70k 29.57，+8.2）→ init 是关键，但未文字特化需微调 |
| 2026-06-22 | ✅ 启动正确 teacher 微调（复刻 0.5B 配方） | init=`VOSR_1.4B_ms` ema、LR **1e-5**、cfg0.5、SA-Text 40k 步、eff16 local2 | config `VOSR_1.4B_ms_from_pretrained.yml`；`run_ms_from_pretrained.sh`（`train_vosr.py`） | 226.31 GPU1-4 / screen `ms_pretrained` | "Loading pretrained"、起步 loss **0.0809**（<<从零 1.3）→ 健康训练中。详 `result_exp.md` 1.4B 节 |
| 2026-06-22 | 激进 Real-CE 微调解锁（dmj 死占 GPU0 3.5h）→ 改 3 卡 | GPU1,2,3、bs3×accum4=eff12、LR1e-4、15k步、real_paired | `run_realce_aggr_local.sh`（改 nproc3）、config bs4→3 | 本机 GPU1-3 / screen `realce_aggr` | loss 0.0027、无 shapely、0/15000→训练中 |
| 2026-06-22 | teacher CharAcc-vs-step 诊断结果：**已平台** | 50k 27.76 / 60k 29.67 / 70k 29.57（同120图）→ 60k 后停涨 | 199 `metric_tr{50000,60000,70000}.py`（坑：harvest 漏 conda activate→重跑） | 199 GPU1 | **续训无用，需换 recipe**（pretrained init / LR / batch）；已问用户。详 `result_exp.md` 1.4B 节 |
| 2026-06-22 | 决策:停重蒸先修 teacher + CharAcc-vs-step 诊断 | 停 chain_sc/teacher847；teacher ckpt-50k/60k/70k×120图多步→CharAcc 趋势 | `teacher_trend_diag.sh`/`harvest_teacher_trend.sh` | 226.31 GPU1/2/3 / screen `teacher_trend`+`harvest_trend` | 诊断中(~80min)→判 CharAcc 还在涨(续训100k)还是平台(换recipe)。详 `result_exp.md` 1.4B 节 |
| 2026-06-22 | ★★ 1.4B teacher 199 协议①（201图）→ 识别欠拟合真根因 | teacher 多步25 vs 0.5B | `metric_teacher1p4b.py`（199 OSEDiff）；全量 `harvest_teacher847_full.sh` | 199 GPU1 / screen `metric_teacher` | **CharAcc 36.82**（PSNR 23.16）<< 0.5B teacher 63.78 → **teacher 识别端严重欠拟合**，蒸馏被封顶；之前"可用"误判=只看PSNR。详 `result_exp.md` 1.4B 节 |
| 2026-06-22 | 1.4B teacher 多步 847 评测（建 teacher 基线，用户要求） | ckpt-70000、25步、cfg0.5、分片 GPU1-4 | `teacher_847_sharded.sh`（inference_vosr.py） | 226.31 GPU1-4 / screen `teacher847` | 推理中（847，~1.8h）→ 拉 199/本机协议①② |
| 2026-06-22 | Real-CE **激进微调**启动（验证"微调过弱"假说，因真实配对证 null） | LR 1e-4（20×）、15000 步、real_paired 13mm、init=2a-GT、在线检测 | config `VOSR_0.5B_realce_2a_realpair_aggressive.yml`；`run_realce_aggr_local.sh`/`wait_then_launch_aggr.sh`（等4卡≥20GB） | 本机 GPU0-3 / screen `realce_aggr`（守护 `wait_aggr`） | 等 GPU0（dmj 占）腾空→自启；停了冗余的温和在线守护 |
| 2026-06-22 | ★ Real-CE 真实配对结果（头号，**假设证伪**） | realpair GT-box ckpt-4500 vs 合成/2a-GT/论文 | `eval_realce.py` 双 json | 本机 GPU0/2 | **真实配对 rec_acc 0.2957 ≈ 合成 0.2954 ≈ 2a-GT 0.2935**，PSNR ~17.4 不动，距论文 0.3093 仍远→**真实配对未打破 null，LQ 来源无差异**；归因=微调过弱(5e-6×5000 几乎不移动)+ 一步蒸馏 vs 从头回归不同物种。详 `result_exp.md` 跨域节 |
| 2026-06-22 | 收割 Real-CE GT-box 两版（合成5000 vs **真实配对**4500）头号对比 | 拉两 ckpt 的 ema+args.json 到本机、本机空卡(GPU0/2)Real-CE val 推理+eval_realce | `infer_realce_local.sh`/`eval_realce.py`；守护 `harvest_realce_gt_variants.sh`(screen `harvest_rce_gt`) | 本机 GPU0(syn)/GPU2(realpair) | 推理中。**坑**：只拉 ema 缺 `args.json`→推理 `ae_type` AttributeError；补拉 args.json(脚本向上5层找)即修。判真实配对 rec_acc 是否>0.2935→0.3093 |
| 2026-06-22 | 跨域回测完成：在线 ft 847 **协议①+②**（原域遗忘检查） | 在线检测 Real-CE 微调 ckpt-5000 回测 real_test 847 | `metric_2a_online_ft.py`(199 OSEDiff)+ `eval_spotting.py`(本机 TESTR) | 199 GPU2 + 本机 GPU2 | ①CharAcc 67.14/PSNR 24.89 ②det 75.68/E2E None 43.86 → **无灾难性遗忘**（vs 2a-GT 持平）。详 `result_exp.md` 跨域节 |
| 2026-06-21 | 跨域回测：在线 ft **协议② TESTR**(847) | TESTR R50 Polygon、totaltext ckpt | `/data/ywk/TAIR/realtext_eval/eval_spotting.py`；out `eval_out/2a_online_ft_5000` | 本机 / GPU2 / screen `testr_2a_online` | **Det F1 75.68 / E2E None 43.86**(vs 2a-GT 原 ~76.6/~41.9：det 微降~1、E2E 微升~2)。详 `result_exp.md` 跨域回测 |
| 2026-06-21 | Real-CE **真实配对**微调（13mm→52mm，正解） | init=2a-GT remote ckpt-20000、teacher=checkpoint-00040000、w=0.5、LR 5e-6、5000 步、ckpt/500、GT-box、resolution 512、eff-batch 16 | 改 `dataloaders/realsr_dataset.py`（+`real_paired_lq_dir`/`_apply_same_crop`/collate 堆 lq）+ `train_vosr_distill.py`（训练环 `batch.get("lq")` 优先真实 LQ 跳过合成退化）；config `VOSR_0.5B_realce_2a_realpair_gt.yml`；脚本 `run_realce_realpair_199.sh`/`wait_then_launch_realpair.sh` | 199 / GPU1-4 / screen `realce_realpair`（守护 `wait_realpair`） | 冒烟通过（645 图/hq≠lq/collate ✓）；合成版结束后接力启动；到 5000 收 Real-CE+847+TAIR。详 `result_exp.md` 跨域节 |
| 2026-06-21 | Real-CE **合成退化**微调收割（在线检测版） | 同上但 LQ=训练环合成退化（**未用真实配对**） | config `VOSR_0.5B_realce_2a_online_finetune.yml`；`run_realce_2a_online_local.sh` | 本机 / GPU0-3 / screen `realce_2a_online` | **负结果**：rec_acc 0.2935→0.2916（微降）；根因=假微调（合成退化）。详 `result_exp.md` 跨域节 |
| 2026-06-21 | Real-CE 表补官方参照行 + HR oracle 评测 | LR/HR/RRDB 基线（Supp Table 1）；HR oracle = 52mm 自评 | `scripts/eval_realce.py`（`--sr_dir val/52mm`） | 本机 / GPU2 | HR oracle 0.4836/0.8353 复现论文 0.4807/0.8342→管线互验。详 `result_exp.md` |
| 2026-06-21 | 跨域回测：在线 ft 847 协议① | 847 SR(NFE=1,u=4)推 199 评测 | `metric_2a_online_ft.py`（改 SR_PATH，派生自 metric_w025） | 199 / GPU2 / screen `metric_2a_online` | 跑中。详 `result_exp.md` 跨域回测 |

> **填表约定**：新操作追加在表首行下方（倒序）；表过长时按月归档到 `doc/agent_operation_log_YYYYMM.md` 并在此保留近一个月 + 指针。一次操作即便失败也要记（含失败原因），与 `result_exp.md` "含失败实验" 约定一致。
